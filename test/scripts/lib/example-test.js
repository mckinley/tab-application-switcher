import helper from '~/test/helper';
import example from '~/app/scripts/lib/example';

describe('example', function() {
  it('should exist', function() {
    expect(example).to.be.an('object');
  });
});
